import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ir.persian.voice.studio',
  appName: 'Persian Voice Studio',
  webDir: 'www',
  bundledWebRuntime: false,
  android: {
    backgroundColor: '#0b0f19'
  }
};

export default config;